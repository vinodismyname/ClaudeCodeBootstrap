"""
Claude Code Project Bootstrapper (cc-bootstrap)
A Python CLI tool for automatically generating Claude Code configuration files.

This tool creates appropriate CLAUDE.md, .claude/commands/, .mcp.json,
and .claude/settings.json files for a project, populated with contextually
relevant content derived from user-provided information and (optionally)
existing project structure.
"""

__version__ = "0.1.0"
