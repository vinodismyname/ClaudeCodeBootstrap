#!/usr/bin/env python3
"""A setuptools based setup module for cc-bootstrap."""

from setuptools import setup

# Use setup.py only for development and direct installs
# For PyPI distribution, we use pyproject.toml
if __name__ == "__main__":
    setup()
